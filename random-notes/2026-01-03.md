1. Cache with hashmap, hashset
	- Hashmap:
		- Load data from database/API
		- Need to find item by ID
		- Track quantities
		- Map relationships
		- Store settings/config
		- Tag filtering
		- Recent items (LRU)
		- Database query results
	- Hashset:
		- Check if something exists
		- Prevent duplicates
		- Track completed actions
		- Filter unique items
		- Check permissions
	- Cache:
		- Check cache **first**
		- Check database, room, local
		- Check API, save to room, cache

2. Multiplaftofm settings
	- KMP data store
	- multiplaftom-settings from russhwolf
3. 