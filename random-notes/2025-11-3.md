1. **Write an Android Studio Plugin**
	- 
2. **Inline variable refactoring**
	- Apply Inline Variable when a **variable is only used once** or when its name does not add any meaningful information. **If the variable does not contribute to code readability or logic, it can be inlined**.
	- Ctrl + Shift + N -> Inline
	- val fileExists = File(path).exists()
	- if (!fileExists)
	- If the method has become redundant or does not add significant value, it can be safely inlined.
	Middle Man code smell: a **class or method acts as a simple intermediary**, forwarding calls to another class or method **without adding any meaningful behavior or logic** of its own
	=:> Use inline variables and inline functions
3. BOM (Bill of Materials) → keeps all library modules under **one shared version**.
	Example:
	- implementation(platform("io.ktor:ktor-bom:3.0.0"))
	- implementation("io.ktor:ktor-client-core") // version auto from BOM
	→ Prevents version mismatch between modules.
	- **Bundle** → groups multiple libraries into one shortcut.
	Example (libs.versions.toml):
	[bundles]
	- ktor-client = ["ktor-client-core", "ktor-client-logging"]
	Usage:
	implementation(libs.bundles.ktor.client)
	🧠 Summary:
	- **BOM** = one version for many libs.
	- **Bundle** = one alias for many deps.
4. **ViewPager2** `**offscreenPageLimit**`
	- Keeps nearby fragments in memory; those beyond the limit call `onDestroyView()` but **not** `onDestroy()`.
	- Fragment object stays in FragmentManager → view recreated when revisited.
    

5. **Edge-to-Edge conflicts**

	- **Issue:** both root and BottomNavigationView apply system insets → **double bottom padding**.

	=> Handle `WindowInsets`**once per view** — top for root, bottom for BottomNavigationView — or disable BNView’s automatic insets if root already pads it.
	6. **Summary:
	- `cornerRadius` → used in traditional `**<shape>**` **drawables** (`GradientDrawable`), controls corner rounding in XML backgrounds.
	- `cornerSize` → used in **Material Components** (`ShapeAppearance` / `MaterialShapeDrawable`), defines corner size within the Material theming system.
	👉 `cornerRadius` = old drawable XML
	👉 `cornerSize` = Material Design shape style
