1. Bug update favorite icon in Adapter
	- Use var 
	- Dont need to use notifyItemChanged
	- Update icon only
2. `PendingIntent.getActivity()` **won’t prevent Activity recreation**.
	- Must add flags to the Intent:
	- `FLAG_ACTIVITY_REORDER_TO_FRONT`
	- Optional: `FLAG_ACTIVITY_SINGLE_TOP

3. Location permisison + service
	- Add a receiver to listen for PROVIDERS_CHANGED_LOCATION
	- fusedLocationProvider.lastLocation.addSuccess, addFailListener
	- fusedLocationProvider.requestLocationUpdates